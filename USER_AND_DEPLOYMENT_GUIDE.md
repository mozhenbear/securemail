# SecureMail 企業安全郵件系統 — 使用者操作與系統部署維運手冊
*(SecureMail User Operations & Enterprise Deployment Guide)*

---

# 📖 目錄 (Table of Contents)

- [第一部分：使用者操作手冊 (User Operations Manual)](#第一部分使用者操作手冊-user-operations-manual)
  - [一、 登入與安全認證](#一-登入與安全認證)
  - [二、 郵件閱讀與隱私安全防護](#二-郵件閱讀與隱私安全防護)
  - [三、 郵件撰寫、超大附件與進階發信](#三-郵件撰寫超大附件與進階發信)
  - [四、 個人偏好設定與多簽名檔管理 (Personal Preferences & Signatures)](#四-個人偏好設定與多簽名檔管理-personal-preferences--signatures)
  - [五、 企業多層安全審核與代理人流程](#五-企業多層安全審核與代理人流程)
  - [六、 行事曆、會議排程與通訊錄](#六-行事曆會議排程與通訊錄)
- [第二部分：管理員與部署維運手冊 (Deployment & Admin Manual)](#第二部分管理員與部署維運手冊-deployment--admin-manual)
  - [一、 系統需求與網路埠口準備](#一-系統需求與網路埠口準備)
  - [二、 Docker Compose 一鍵快速部署](#二-docker-compose-一鍵快速部署)
  - [三、 商業授權啟用與私有化管理](#三-商業授權啟用與私有化管理)
  - [四、 企業網域與 DNS 信譽配置 (SPF / DKIM / DMARC)](#四-企業網域與-dns-信譽配置-spf--dkim--dmarc)
  - [五、 企業 LDAP / Active Directory 目錄同步](#五-企業-ldap--active-directory-目錄同步)
  - [六、 企業單一登入 (SSO - SAML 2.0 / Azure AD / Google)](#六-企業單一登入-sso---saml-20--azure-ad--google)
  - [七、 全量郵件合規歸檔庫、法規保全與原信存證 (Compliance Archiving & Legal Hold)](#七-全量郵件合規歸檔庫法規保全與原信存證-compliance-archiving--legal-hold)
  - [八、 全系統安全操作審計日誌與合規清理 (Audit Logs & Retention)](#八-全系統安全操作審計日誌與合規清理-audit-logs--retention)
  - [九、 系統健康度監控與發信佇列管理](#九-系統健康度監控與發信佇列管理)
  - [十、 多租戶自訂企業品牌 (Branding)](#十-多租戶自訂企業品牌-branding)
  - [十一、 全企業商務簽名檔公版管理與動態變數字典 (Corporate Signature Template)](#十一-全企業商務簽名檔公版管理與動態變數字典-corporate-signature-template)
  - [十二、 資料備份與災難復原策略](#十二-資料備份與災難復原策略)
  - [十三、 系統管理員密碼忘記或帳號鎖定之緊急救援 (Admin Emergency Rescue)](#十三-系統管理員密碼忘記或帳號鎖定之緊急救援-admin-emergency-rescue)

---

# 第一部分：使用者操作手冊 (User Operations Manual)

## 一、 登入與安全認證

### 1. 一般帳密登入
1. 打開瀏覽器訪問 Webmail 網址（例如：`http://mail.yourcompany.com` 或 `http://localhost:33333`）。
2. 輸入完整企業信箱（例如：`user1@yourcompany.com`）與密碼，點擊【登入信箱】。
3. **密碼錯誤防護**：連續輸入錯誤達 5 次時，系統將自動鎖定帳號以防暴力破解，請聯繫系統管理員解鎖。

### 2. 企業單一登入 (SSO)
1. 若公司已啟用企業 SSO（如 Microsoft 365 / Azure AD 或 Google Workspace），請點擊登入頁下方的【**使用企業 SSO 單一登入**】按鈕。
2. 系統將引導跳轉至企業身分驗證頁面，完成驗證後自動回到信箱主畫面。

### 3. 雙因子動態驗證 (TOTP 2FA) 設定
1. 登入後點擊頂部導航列【🎛️ **個人偏好設定**】。
2. 切換至【雙因子動態驗證 (2FA)】區塊，點擊【設定 2FA 驗證器】。
3. 開啟手機端驗證器 App（如 Google Authenticator、Microsoft Authenticator），掃描 QR Code 或輸入 Secret 金鑰。
4. 輸入 App 產生的 6 位數動態驗證碼並點擊【驗證並啟用】。日後登入時將需要同時輸入密碼與 6 位數動態碼。

---

## 二、 郵件閱讀與隱私安全防護

### 1. 外部圖片預設阻擋與信任名單 (Anti-Tracking)
- **原理**：為防止外部寄件人利用 1x1 像素透明追蹤圖（Web Beacon）刺探您的 IP 位址、開信時間與所在位置，系統預設將所有外網圖片轉為灰色佔位圖。
- **單次載入**：點擊信件頂部安全橫幅中的【**本次顯示圖片**】，即可立即載入該信件的所有外部圖片。
- **永遠信任**：點擊【**永遠信任此寄件人**】，該寄件人日後寄來的所有信件將自動顯示圖片。
- **偏好調整**：可在【個人偏好設定】中隨時管理信任寄件人名單或切換預設模式。

### 2. 垃圾郵件與釣魚防毒警示 (Spam & Virus Guard)
- 當收到可疑郵件（包含危險副檔名如 `.exe`, `.vbs` 或符合釣魚特徵）時，系統會在信件頂部以紅色醒目橫幅標註「**⚠️ 資安防護警示**」，並自動摺疊可疑附件防止誤點中毒。

### 3. Web 專用 DLP 防洩密動態浮水印 (Dynamic Anti-Leakage Watermark)
- **動態浮水印呈現**：閱讀信件時，畫面會動態疊加包含您**中文姓名、帳號 Email 與即時閱讀精確時間戳 (YYYY/MM/DD HH:mm)** 的半透明浮水印，呈 -18 度矩陣平鋪於最頂層，有效防範手機拍照與螢幕截圖洩密。
- **全相容頂層遮罩**：浮水印採用純前端 SVG 與非侵入式穿透疊層 (`pointer-events: none`)，覆蓋於所有表格、圖片與字體上方，**完全不影響滑鼠點擊、文字反白選取與捲動**。
- **後台全域開關**：系統管理員可於管理後台【品牌外觀】中隨時「開啟」或「停用」全系統浮水印功能，開關變更後 Webmail 讀信端即時動態生效。

### 4. 郵件右鍵快捷選單、勾選批次操作與單信快速移動
- **右鍵選單 (Context Menu)**：在任意郵件項目上按右鍵，即可呼叫全功能捷徑選單：
  - 支援快速回覆/全部回覆/轉發、標記已讀或未讀、標記星號、刪除郵件。
  - **移動至資料夾**：一體化呈現所有系統與自訂資料夾（自動排除目前所在資料夾）；內建視窗邊界偵測，當右鍵點擊靠近螢幕底部的郵件時，選單將智慧向上翻轉展開（Upward Flip），絕不被螢幕遮擋。
- **勾選批次移動 (Batch Move)**：勾選多封郵件後，列表頂部工具列之【移動至...】下拉選單自動同步所有自訂資料夾並排除當前資料夾，一鍵批次歸檔。
- **閱讀器單信移動 (Reader Move)**：於信件閱讀視圖頂部動作列，可直接透過【📁 移動至...】下拉選單將該信件移入指定資料夾並自動返回。

### 5. 自訂資料夾管理與自動分類規則 (Custom Folders & Filter Rules)
- **建立與管理自訂資料夾**：
  - 於左側側邊欄「自訂資料夾」區塊點擊【➕】按鈕即可快速建立新資料夾（如「專案 A」、「VIP 客戶」）。
  - 滑鼠移至自訂資料夾上方，可點擊圖示進行【更名】或【刪除】；系統核心預設資料夾（收件箱、草稿箱、已發送、待審核等）受白名單防護，安全鎖定禁止刪改。
- **自動分類規則 (Filter Rules)**：
  - 點擊頂部【⚡ 自動分類規則】可建立多條件過濾邏輯（如「寄件人包含 @partner.com」且「主旨包含 報價」）。
  - 動作可選擇【移動至指定資料夾】（支援所有系統與自訂資料夾）；系統在背景自動即時分流，並完整保留新信原始未讀狀態與資料夾未讀徽章。

---

## 三、 郵件撰寫、超大附件與進階發信

### 1. 主畫面即時編輯器 (In-place Composer)
- 點擊左側【**寫郵件**】，主畫面直接開啟全功能富文本編輯器。
- **智慧草稿自動儲存**：輸入中每 1.5 秒自動背景同步至雲端草稿箱 (`Drafts`)，切換頁面不遺失。
- **插入圖片與縮放**：工具列點擊【圖片】可插入圖片；點選圖片後可透過懸浮工具列切換 25%、50%、75%、100% 比例，或使用滑鼠邊緣拖曳縮放。

### 2. 寫信即時切換簽名檔
- 撰寫新信、回覆或轉發郵件時，系統會自動在內文底部或引用內容上方置入您的預設簽名檔。
- 在富文本工具列右側點擊 **【✍️ 簽名】下拉選單**，可隨時即時更換不同簽名檔（如商務簽名、英文簽名）、選擇【🚫 不使用簽名檔】，或點選【⚙️ 管理簽名檔...】直接開啟個人設定。

### 3. 發信 5 秒緩衝撤回 (Undo Send)
- 點擊【發送】後，畫面底部會彈出 **5 秒倒數懸浮橫幅**。
- 若發現漏附檔案或收件人有誤，點擊【**撤回 (Undo)**】可立即中斷發信並還原編輯器內容。

### 4. 超大附件雲端下載卡片 (Big Attachment Sharing)
- 欲寄送數百 MB 或數 GB 的大檔案時，在附件區點擊【**+ 插入超大附件 (轉雲端下載連結)**】。
- 選擇檔案、下載有效天數（7/14/30/90天）與可選的提取密碼，點擊上傳。
- 系統將檔案儲存於雲端空間，並自動在信件內文插入精美的「**超大附件雲端下載卡片**」，收件人點擊即可安全下載。

### 5. 預約定時發信 (Scheduled Send)
- 在編輯器右上角點擊【**⏱️ 預約發信**】。
- 選擇明天上午 (09:00)、明天下午 (14:00) 或自訂未來特定時間，點擊確定。
- 系統將於指定時間由背景自動排程發送，可隨時在【預約發信】清單中檢視或取消。

---

## 四、 個人偏好設定與多簽名檔管理 (Personal Preferences & Signatures)

點擊頂部導航列【🎛️ **個人偏好設定**】即可開啟個人化綜合控制面板：

### 1. 個人郵件簽名檔管理
- **多簽名檔支援**：可自由建立多組不同語系或場合的專屬簽名（例如：「正式商務簽名」、「英文名片簽名」、「行動端簡短簽名」）。
- **視覺所見即所得（WYSIWYG）富文本編輯**：簽名檔編輯器提供字型加粗、斜體、底線、刪除線、文字與背景顏色、段落對齊、插入超連結以及插入公司 Logo/圖片功能，無需編寫任何 HTML 程式碼。
- **【✨ 套用商務公版範本】**：點擊範本按鈕，系統將自動擷取管理員在後台設定的統一公版，並動態帶入您的姓名、職稱、部門、電話與公司網域。
- **預設簽名指派**：可將常用簽名設為「預設簽名檔」，寫信時系統將自動置入。

### 2. 休假自動回覆 (Out of Office)
- 勾選【啟用休假期間自動回覆】，設定請假/出差之起訖時段，並自訂回覆主旨與說明內文。設定期間內收到的來信將自動收到您的排程回覆。

### 3. 時區與外部圖片偏好
- **偏好時區**：支援切換台北/香港 (UTC+8)、東京 (UTC+9)、美東、美西、UTC 等，信件與日誌將依據所選時區精準轉換時間。
- **圖片載入與信任名單**：可設定外部圖片預設行為，並檢視或移除已信任的外部寄件人。

---

## 五、 企業多層安全審核與代理人流程

### 1. 審核觸發條件
若發送信件命中以下條件之一，系統將自動暫扣信件並指派給主管審核：
- **外網郵件審核**：收件人包含非公司網域之外部信箱。
- **附件郵件審核**：信件帶有特定大小或類型的附件。
- **敏感關鍵字審核**：主旨或內文包含公司設定的敏感詞彙（如「報價」、「合同」）。

### 2. 發件人端流程
- 送出信件後，信件進入【**待審核 (`Approval`)**】資料夾，標註 `【送審中】`。
- **審核通過**：信件自動送往收件人，您會收到「✔ 審核通過確認回執」，信件移至【已審核通過 (`Approvaled`)】。
- **審核退回**：信件移至【已拒絕 (`Rejected`)】，您會收到退回通知信與主管退回理由。點擊信件工具列中的【**重新編輯**】即可修正後再次送出。

### 3. 主管審核與出差代理人設定
- **主管審核**：登入後在【待審核】資料夾點開信件，工具列提供【**✔ 核准發送**】與【**✖ 駁回**】（可填寫退回原因）。
- **出差/休假代理人指派**：
  - 進入【個人選單】 ➔ 【審核代理人設定】。
  - 勾選啟用代理，指定代理人信箱（例如：`agent@yourcompany.com`）與代理起訖時間。
  - 在代理期間內，所有待審郵件將精確自動轉派給代理人審核，主管銷假後自動恢復。

---

## 六、 行事曆、會議排程與通訊錄

### 1. 行事曆與團隊會議邀請
- 點擊左側導覽列【**行事曆**】，支援日、週、月視圖切換。
- 點選日期建立行程，支援每日/每週/每月週期循環。
- 勾選【**團隊會議**】並輸入與會者信箱，系統將自動發送標準 **RFC 5545 iCalendar (`.ics`)** 會議邀請信給所有成員，並即時追蹤成員出席狀態（接受/拒絕/暫定）。

### 2. 企業通訊錄與寫信自動補全
- 點擊左側【**通訊錄**】可檢視公司組織同仁與個人聯絡人。
- 在寫信介面輸入姓名或帳號關鍵字時，收件人欄位會自動即時補全相關聯絡人。

---

# 第二部分：管理員與部署維運手冊 (Deployment & Admin Manual)

## 一、 系統需求與網路埠口準備

### 1. 最低伺服器硬體規格
- **CPU**：2 核心以上（推薦 4 核心以上）
- **RAM**：4 GB 以上（推薦 8 GB 以上）
- **磁碟空間**：50 GB SSD 以上（依郵件量與大附件保存需求評估）
- **作業系統**：Ubuntu 20.04/22.04 LTS、Debian 11/12、RHEL/AlmaLinux 8/9 或 macOS (Docker Desktop)

### 2. 防火牆與通訊埠開放清單
| 通訊埠 (Port) | 協定 | 服務名稱 | 說明 |
| :--- | :--- | :--- | :--- |
| **25** | TCP | Postfix SMTP | 網際網路標準郵件交換埠（發信與收信互聯） |
| **587** | TCP | Postfix Submission | 客戶端加密發信埠 (STARTTLS) |
| **143** | TCP | Dovecot IMAP | 內部 IMAP 連線埠 |
| **993** | TCP | Dovecot IMAPS | SSL/TLS 加密 IMAP 埠 |
| **33333** (或 3000) | TCP | SecureMail Web/API | Webmail 與管理後台 HTTP 存取埠 |
| **3306** | TCP | MySQL 資料庫 | （建議僅供本機/內部 Docker 網路存取） |

---

## 二、 Docker Compose 一鍵快速部署

本系統遵循「**容器純執行環境**」架構，所有資料庫檔案、Maildir 郵件目錄與日誌均持久化儲存於宿主機本地目錄：

### 1. 複製專案與檢查目錄結構
```bash
git clone https://onedev.zodic.dev/securemail.git
cd securemail/modern-mail
```

### 2. 一鍵建置並啟動所有容器
```bash
docker compose up -d --build
```

### 3. 檢查容器運行狀態
```bash
docker compose ps
```
確認 `modern-mail-web`、`modern-mail-postfix`、`modern-mail-dovecot`、`modern-mail-mysql` 均處於 `Up` 狀態。

### 4. 訪問入口
- **使用者 Webmail 介面**：`http://<您的伺服器IP>:33333/`
- **系統管理後台 (Admin Portal)**：`http://<您的伺服器IP>:33333/admin`
  - 預設管理員帳號：`admin`
  - 預設管理員密碼：`admin123`

---

## 三、 商業授權啟用與管理 (Commercial Licensing & Fallback Guide)

SecureMail 系統採用**雙重授權模式 (Dual Licensing Model)**：
- **社群免費版 (Community Free)**：系統首次安裝或未安裝商業授權時，預設啟用 5 帳號永久免費授權，所有核心收發、審核、行事曆與通訊錄功能皆可永久使用。
- **商業正式版 (Enterprise Commercial)**：若欲擴充帳號配額或取得原廠技術支援，僅需提供伺服器機器碼向原廠取得專屬授權。

### 1. 商業授權過期自動平滑回退 (Graceful Fallback Mechanism)
- **平滑回退**：若已安裝的商業授權檔案過期或無效，系統**不會強制中斷服務或鎖死信箱**，而是**自動平滑跌回「社群免費版（上限 5 帳號、永久有效）」**。
- **現有帳號保護**：現有通訊、收發信與既有信箱資料完全不受影響；僅當使用中帳號超出 5 人時暫停建立新帳號。

### 2. 系統已安裝授權清單與狀態檢視
1. 登入管理後台 (`/admin`) ➔ 進入【**商業授權管理**】分頁。
2. 於【**系統已安裝授權清單與生效狀態**】表格中，可即時檢視系統中登記的所有授權：
   - 🟢 **當前生效中 (Active)**：目前系統套用的主要有效授權與配額。
   - 🔴 **已過期 (Expired)**：過期商業授權，系統已自動平滑降級回退為社群版。
   - ⚠️ **異常 (Invalid)**：機器碼不符或數位簽章異常之授權。
3. **一鍵移除舊授權**：針對已過期或廢棄的商業授權檔，管理員可點擊【移除】按鈕清理記錄。

### 3. 機器碼取得與授權匯入步驟
1. 進入【**商業授權管理**】分頁。
2. 點擊【**複製機器碼**】按鈕（格式為 `SECUREMAIL-XXXX-XXXX-XXXX-XXXX`，由伺服器本地自動採集硬體特徵生成，支援完全斷網環境）。
3. 將機器碼提供給發行商/商務代表（信箱：`mozhenbear@zodic.dev`）。
4. 取得專屬授權金鑰字串或 `.lic` 檔案後，於【**匯入新商業授權檔案 / 金鑰**】中貼上或上傳檔案。
5. 點擊【**驗證並套用授權**】，授權即時生效（無需重啟容器）。

---

## 四、 企業網域與 DNS 信譽配置 (SPF / DKIM / DMARC)

為確保企業郵件不會被 Gmail、Outlook 等國際信箱判定為垃圾郵件，請依下列指引設定 DNS：

### 1. 後台 DKIM 2048-bit 金鑰產生
1. 進入管理後台 ➔ 【**網域信譽與 DNS 檢測**】分頁。
2. 點擊【**一鍵生成 DKIM 2048-bit 公私鑰**】，系統將自動寫入資料庫。

### 2. 公網 DNS 記錄設定表 (以 `yourcompany.com` 為例)
| 記錄類型 | 主機名稱 (Name) | 記錄值 (Value) | 優先級 (Priority) |
| :--- | :--- | :--- | :--- |
| **A** | `mail.yourcompany.com` | `<您的伺服器公網 IP>` | - |
| **MX** | `@` 或 `yourcompany.com` | `mail.yourcompany.com` | `10` |
| **TXT (SPF)** | `@` | `v=spf1 mx a ip4:<您的伺服器IP> ~all` | - |
| **TXT (DKIM)** | `default._domainkey` | `v=DKIM1; k=rsa; p=<後台生成的公鑰字串>` | - |
| **TXT (DMARC)**| `_dmarc` | `v=DMARC1; p=quarantine; rua=mailto:dmarc-reports@yourcompany.com` | - |

### 3. 線上即時 DNS 診斷
在後台【網域信譽與 DNS 檢測】面板點擊【**診斷目前 DNS 解析**】，系統將即時查詢公網 DNS 並回報各項記錄是否生效。

---

## 五、 企業 LDAP / Active Directory 目錄同步

1. 進入管理後台 ➔ 【**企業 LDAP / AD 同步**】分頁。
2. 配置企業目錄伺服器參數：
   - **LDAP 伺服器位址**：例如 `ldap://192.168.1.100:389` 或 `ldaps://ad.company.com:636`
   - **管理員 Bind DN**：例如 `cn=admin,dc=company,dc=com` 或 `admin@company.com`
   - **Bind 密碼**：輸入 AD/LDAP 查詢帳號之密碼
   - **搜尋根節點 (Base DN)**：例如 `ou=Employees,dc=company,dc=com`
   - **使用者過濾條件**：例如 `(&(objectClass=user)(mail=*))`
   - **同步關聯網域**：選擇同步後員工帳號所屬的郵件網域
3. 點擊【**儲存 LDAP 設定**】。
4. 點擊【**立即手動同步 LDAP 組織**】驗證連線，同步結果將顯示新增、更新與略過之員工帳號數。

---

## 六、 企業單一登入 (SSO - SAML 2.0 / Azure AD / Google)

1. 進入管理後台 ➔ 【**企業單一登入 (SSO)**】分頁。
2. 選擇身分提供者類型（`Microsoft Azure AD`、`Google Workspace`、`Okta` 或 `自訂 OIDC`）。
3. 填入在 Azure/Google 開發者後台申請的：
   - **Client ID** (應用程式識別碼)
   - **Client Secret** (應用程式密鑰)
   - **Authorization URL** (授權端點)
   - **Token URL** (權杖端點)
   - **UserInfo URL** (使用者資訊端點)
4. 勾選【**啟用此 SSO 單一登入通道**】並點擊儲存。
5. 前端登入頁即刻啟用 SSO 快速通道按鈕。

---

## 七、 全量郵件合規歸檔庫、法規保全與原信存證 (Compliance Archiving & Legal Hold)

1. 進入管理後台 ➔ 【**合規歸檔**】分頁。
2. **不可篡改存證 (Immutable WORM)**：所有進出系統之郵件本體均即時計算 SHA-256 原文雜湊並全量歸檔存證。即使發件人或收件人在個人信箱中執行「徹底刪除」或「清空垃圾桶」，歸檔庫均永久完好保留。
3. **原信存證預覽與 EML 司法鑑識匯出**：
   - 點擊任一歸檔郵件列或點擊【**👁️ 檢視存證**】，可開啟彈窗即時切換預覽【郵件內容】與【RFC822 原文 (EML Raw)】（包含完整 MIME 標頭與 DKIM 簽章）。
   - 點擊【**📥 下載 RFC822 原信 (.eml)**】可一鍵將未經修改之原信匯出至本機以供司法調查與第三方鑑識工具檢驗。
4. **法規保全凍結 (Legal Hold / 訴訟保全)**：
   - 面臨訴訟或內部重大稽核調查時，點選目標郵件旁之【**法規凍結**】並輸入保全理由。
   - 凍結後該郵件將受最高層級防刪除保護，永久豁免於任何自動保留清理週期。

---

## 八、 全系統安全操作審計日誌與合規清理 (Audit Logs & Retention)

1. 進入管理後台 ➔ 【**安全操作審計日誌**】分頁。
2. **全流程特權操作監控**：自動即時記錄管理員登入、帳號增刪改、網域管理、審核特權放行/退回、LDAP 同步、授權更新、佇列強制投遞等操作之人員、來源 IP、時間戳與詳細變更內容。
3. **符合 ISO 27001 / SOC 2 之年限合規清理機制 (Retention Policy)**：
   - 系統鎖定保護**最近 1 年內之活躍操作日誌**，禁止任意單筆刪除以維護不可抵賴性。
   - 點擊右上角【**🗑️ 合規日誌清理**】，可依據企業法規要求選擇清除 1 年前、2 年前或 3 年前之歷史舊日誌。
   - 每次清理動作本身將作為高權限審計事件（`LOG_PURGE`）自動永久存證於日誌中。

---

## 九、 系統健康度監控與發信佇列管理 (System Health & Queue)

1. 進入管理後台 ➔ 【**系統健康與發信佇列**】分頁。
2. **即時指標**：
   - CPU 核心數與系統負載平均值 (Load Average)
   - 記憶體總量、已用量與使用百分比
   - Maildir 郵件儲存池磁碟空間容量與使用率
   - 系統運行時間 (Uptime) 與 Node.js 執行環境
3. **發信佇列狀態 (Mail Queue Breakdown)**：
   - 監控 Active (發送中)、Deferred (延遲重試) 與 Hold (審核留置) 佇列數量。
4. **一鍵重新投遞 (Flush Queue)**：
   - 遇外部網路中斷恢復後，點擊【**強制重新投遞佇列 (Flush Queue)**】，系統將立即重發所有佇列郵件。

---

## 十、 多租戶自訂企業品牌與資安防護開關 (Branding & Security)

### 1. 多租戶企業自訂品牌
1. 進入管理後台 ➔ 【**多租戶自訂品牌**】分頁。
2. 選擇目標網域，可針對不同客戶或子公司自訂：
   - **企業名稱**（例如：`台積電企業郵件系統`）
   - **自訂 Logo 圖片 URL**
   - **登入頁主標題與副標題**
   - **系統主題主色調 (Hex Color)**
   - **頁尾版權宣告文字**
3. 點擊【儲存品牌外觀設定】，前端登入頁與標題將即時呈現自訂品牌形象。

### 2. Web DLP 動態防洩密浮水印安全開關
1. 於【多租戶自訂品牌】分頁下方之【**Web DLP 動態防洩密浮水印安全開關**】卡片。
2. 切換開關「啟用 Webmail 讀信動態防翻拍浮水印」：
   - **開啟 (預設)**：員工讀信時自動平鋪包含員工姓名、帳號與即時時間戳的防翻拍浮水印。
   - **停用**：關閉全系統讀信浮水印，Webmail 讀信端將不再顯示任何浮水印。
3. 點擊【**儲存浮水印設定**】，設定即時生效並自動寫入審計日誌。

---

## 十一、 全企業商務簽名檔公版管理與動態變數字典 (Corporate Signature Template)

管理員可在此自訂全公司統一的標準商務簽名範本，員工在 Webmail 個人偏好設定中點擊【✨ 套用商務公版範本】時，系統將自動解析動態變數並帶入員工真實資訊。

### 1. 支援的動態佔位變數字典

| 動態變數標籤 | 說明 | 範例帶入值 |
| :--- | :--- | :--- |
| `{{name}}` / `{{display_name}}` | 員工姓名（優先帶入中文姓名，其次為暱稱或信箱前綴） | 王大明 |
| `{{email}}` | 員工公務電子郵件地址 | `user1@sectest.com` |
| `{{department}}` | 員工登記之組織部門名稱 | 技術管理部 |
| `{{job_title}}` | 員工職位或職稱 | 資深工程師 |
| `{{phone}}` / `{{mobile}}` | 員工公務手機或聯絡電話 | `0912-345-678` |
| `{{company}}` / `{{company_name}}` | 企業或組織名稱抬頭 | SecureMail 企業安全郵件 |
| `{{domain}}` | 郵件所屬網域名稱 | `sectest.com` |
| `{{company_tel}}` | 企業官方總機代表號 | `+886-2-2345-6789` |
| `{{company_address}}` | 企業營運或辦公室營業地址 | 台北市信義區信義路五段7號 |

### 2. 操作說明
1. 進入管理後台 ➔ 點選頂部導航列【**企業簽名檔公版**】。
2. **一鍵插入變數**：在頂部變數說明卡片中，點擊任何變數標籤（如 `{{name}}`），即可直接插入到目前編輯游標處。
3. **雙模編輯與圖片置入**：支援所見即所得富文本排版（字型顏色、置中、超連結、公司 Logo URL 圖片），亦可切換為【HTML 源碼編輯】進行精準代碼微調。
4. **即時模擬預覽**：下方提供以測試人員為視角的即時解析預覽。
5. **恢復官方預設**：若需重新設計，點擊右上角【恢復官方預設公版】即可一鍵重設。
6. 點擊【**儲存企業簽名檔公版**】，變更將自動記錄於安全操作審計日誌中。

---

## 十二、 資料備份與災難復原策略

為保障企業資料資產安全，建議維運人員設定每日自動排程備份：

### 1. 備份目標目錄
- **MySQL 資料庫實體**：`modern-mail/data/mysql/`
- **Maildir 郵件目錄**：`modern-mail/data/mail/`
- **超大附件儲存目錄**：`modern-mail/backend/data/big_attachments/`

### 2. 定時 SQL 傾印備份指令 (Crontab 範例)
```bash
# 每日凌晨 03:00 自動匯出完整資料庫
0 3 * * * docker compose exec -T mysql mysqldump -u securemail -psecuremail_pass securemail > /backup/securemail_$(date +\%Y\%m\%d).sql
```

### 3. 災難復原 (Disaster Recovery)
若需遷移或在全新主機還原：
1. 將備份的 `data/` 目錄與 SQL 檔案複製至新主機。
2. 執行 `docker compose up -d --build` 重新啟動所有服務。
3. 系統將自動掛載既有郵件目錄並執行資料庫結構自檢遷移。

---

## 十三、 系統管理員密碼忘記或帳號鎖定之緊急救援 (Admin Emergency Rescue)

若系統管理員（`admin`）忘記密碼或因多次密碼錯誤觸發防暴力破解鎖定（`account_locked = 1`），具備伺服器主機權限之維運人員可直接透過以下指令重設密碼並解除鎖定：

### 方式一：透過官方 CLI 工具重設（推薦）
```bash
# 語法：docker compose exec web npm run admin:reset -- <管理員帳號> <新自訂強密碼>
# 範例：將 admin 密碼重設為 MyStrongPass#2026 並解除鎖定
docker compose exec web npm run admin:reset -- admin "MyStrongPass#2026"
```

### 方式二：透過資料庫指令直接解鎖
```bash
docker compose exec -T mysql mysql -u"${MYSQL_USER:-securemail}" -p"${MYSQL_PASSWORD:-securemail_pass}" "${MYSQL_DATABASE:-securemail}" -e "
UPDATE admin_user 
SET password_hash = MD5('MyStrongPass#2026'), 
    account_locked = 0, 
    login_error_count = 0, 
    enabled = 1 
WHERE username = 'admin';
"
```

> **🔒 資安維運提示：**
> 1. 請確保宿主機 SSH 與 Docker 存取權限僅授權給受信任之核心維運人員。
> 2. 救援完成登入管理後台 (`/admin`) 後，請立即前往【安全性設定】綁定 2FA 動態驗證器以強化防護。
> 3. 每次 CLI 救援動作均會自動記錄於系統安全審計日誌（Audit Log）中以供備查。

